<% var hasCheck = ('undefined' !== typeof bsChecks); %>

<% if (hasCheck) { %>
	<% var check = ''; %>
	<% for (var i = 0; i < bsChecks.length; i++) { %>
		  <% check += bsChecks[i]; %>
	<% } %>
{% if ( <%= check %> ) %}
<% } %>

<script>
  TVSite = window.TVSite || {};
  <% if ('object' === typeof bs) { %>
  	<% for ( var bsItem in bs ) { %>
  		TVSite[ '<%= bsItem %>' ] = <%= bs[ bsItem ] %>;
  	<% } %>
  <% } %>
</script>

<% if (hasCheck) { %>
{% endif %}
<% } %>